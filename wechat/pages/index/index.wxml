<!--index.wxml-->
<view class="container">
  	
	<view wx:for-items="{{gameList}}" class="usermotto">
	    <button type="primary" disabled="{{disabled}}" bindtap="start{{item}}"> {{item}} </button>
	</view>

	<text class="more">面向过程</text>
</view>
<view>

</view>
